# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# OpenClaw Gateway (aliases — gateway still reads CLAWDBOT_* internally)
OPENCLAW_GATEWAY_URL=http://127.0.0.1:18789
OPENCLAW_GATEWAY_TOKEN=<token>

# Cloudflare Tunnel — set this in production (Netlify) to reach the local gateway remotely.
# When set, this takes priority over OPENCLAW_GATEWAY_URL.
# Leave unset for local development.
# OPENCLAW_GATEWAY_TUNNEL_URL=https://<tunnel-subdomain>.cfargotunnel.com
OPENCLAW_CANONICAL_SESSION_KEY=agent:main:main
OPENCLAW_TELEGRAM_ECHO_TARGET=<telegram-target>

# Basic Access Control (Phase 4) — set to enable password gate on the deployed site.
# Leave unset to disable password protection (e.g. for local development).
# SITE_PASSWORD=<your-shared-password>

# Legacy (backward compat — used if OPENCLAW_* not set)
# CLAWDBOT_GATEWAY_URL=http://127.0.0.1:18789
# CLAWDBOT_GATEWAY_TOKEN=<token>
# CLAWDBOT_CANONICAL_SESSION_KEY=agent:main:main
# CLAWDBOT_TELEGRAM_ECHO_TARGET=<telegram-target>
