# Mission Control → Clawdbot Gateway (server-side only)
# Copy to .env.local and fill values.

# Preferred env vars:
CLAWDBOT_GATEWAY_URL=http://127.0.0.1:18789
CLAWDBOT_GATEWAY_TOKEN=

# Canonical chat session (Option B): mirror the Telegram DM session.
# Default (if omitted): agent:main:main
CLAWDBOT_CANONICAL_SESSION_KEY=agent:main:main

# Optional: also echo Mission Control user inputs into Telegram so the visible
# Telegram thread matches Mission Control (sent as a Gideon note).
CLAWDBOT_TELEGRAM_ECHO_TARGET=8050931550

# Backwards-compatible fallbacks (still supported):
CLAWDBOT_API_URL=http://127.0.0.1:18789
CLAWDBOT_API_TOKEN=
